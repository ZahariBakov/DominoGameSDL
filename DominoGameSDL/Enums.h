#ifndef ENUMS_H
#define ENUMS_H

enum class GameFlag { Welcome, MainMenu, Playing, Win };
enum class PlayerFlag { First, Second };
enum class Difficulty { Easy, Normal, Hard, All, None };
enum class DominoType { Classic, Vehicle, Flowers, Butterflies, None };
enum class ClassicTileType { White, Black, None };

#endif // ENUMS_H
